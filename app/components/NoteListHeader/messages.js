/*
 * NoteListHeader Messages
 *
 * This contains all the text for the NoteListHeader component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.NoteListHeader.header',
    defaultMessage: 'Number of saved notes: {count}',
  },
});
